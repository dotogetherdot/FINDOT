<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>찾았닷 | 일상 속 점자 찾기</title>

  <style>
    :root {
      --green: #03c75a;
      --yellow: #ffd84d;
      --black: #111111;
      --gray-900: #222222;
      --gray-700: #555555;
      --gray-300: #dddddd;
      --gray-100: #f5f5f5;
      --cream: #fffdf4;
      --white: #ffffff;
      --radius: 18px;
    }

    * {
      box-sizing: border-box;
    }

    body {
      margin: 0;
      font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
      background: var(--cream);
      color: var(--black);
      line-height: 1.6;
      word-break: keep-all;
    }

    a {
      color: inherit;
      text-decoration: none;
    }

    .skip-link {
      position: absolute;
      left: -9999px;
      top: 8px;
      background: var(--black);
      color: var(--white);
      padding: 10px 14px;
      border-radius: 8px;
      z-index: 999;
    }

    .skip-link:focus {
      left: 8px;
    }

    .page {
      width: min(100%, 480px);
      margin: 0 auto;
      padding: 18px 16px 32px;
    }

    .hero {
      padding: 22px 18px 20px;
      border: 2px solid var(--black);
      border-radius: 24px;
      background: var(--white);
      box-shadow: 6px 6px 0 var(--black);
    }

    .badge {
      display: inline-block;
      padding: 5px 10px;
      background: var(--yellow);
      border: 2px solid var(--black);
      border-radius: 999px;
      font-size: 14px;
      font-weight: 800;
      margin-bottom: 12px;
    }

    h1 {
      margin: 0;
      font-size: 34px;
      line-height: 1.15;
      letter-spacing: -0.04em;
    }

    .hero p {
      margin: 10px 0 0;
      color: var(--gray-700);
      font-size: 17px;
      font-weight: 600;
    }

    .search-box {
      margin-top: 18px;
      border: 3px solid var(--green);
      border-radius: 999px;
      padding: 12px 16px;
      display: flex;
      align-items: center;
      gap: 8px;
      font-weight: 800;
      color: var(--gray-700);
      background: var(--white);
    }

    .search-box span:first-child {
      color: var(--green);
      font-weight: 900;
    }

    .section {
      margin-top: 18px;
      padding: 18px;
      border: 1px solid var(--gray-300);
      border-radius: var(--radius);
      background: var(--white);
    }

    .section h2 {
      margin: 0 0 8px;
      font-size: 22px;
      line-height: 1.3;
      letter-spacing: -0.03em;
    }

    .section p {
      margin: 0 0 12px;
      color: var(--gray-700);
      font-size: 15px;
    }

    .button {
      display: block;
      width: 100%;
      margin-top: 10px;
      padding: 15px 16px;
      border: 2px solid var(--black);
      border-radius: 14px;
      background: var(--black);
      color: var(--white);
      text-align: center;
      font-size: 17px;
      font-weight: 850;
      cursor: pointer;
    }

    .button.secondary {
      background: var(--white);
      color: var(--black);
    }

    .button.green {
      background: var(--green);
      color: var(--black);
    }

    .braille-card {
      margin-top: 12px;
      border: 2px dashed var(--gray-300);
      border-radius: 16px;
      background: var(--gray-100);
      overflow: hidden;
    }

    .braille-card img {
      display: block;
      width: 100%;
      height: auto;
    }

    .placeholder {
      padding: 28px 16px;
      text-align: center;
      color: var(--gray-700);
      font-weight: 700;
    }

    .mini-list {
      display: grid;
      gap: 8px;
      margin: 12px 0 0;
      padding: 0;
      list-style: none;
    }

    .mini-list li {
      padding: 10px 12px;
      border-radius: 12px;
      background: var(--gray-100);
      font-weight: 700;
    }

    .challenge {
      background: var(--yellow);
      border: 2px solid var(--black);
    }

    .tagline {
      margin-top: 8px;
      font-size: 18px;
      font-weight: 900;
    }

    details {
      margin-top: 10px;
      border-radius: 14px;
      background: var(--gray-100);
      padding: 12px 14px;
    }

    summary {
      cursor: pointer;
      font-weight: 850;
    }

    .footer {
      margin-top: 24px;
      padding: 16px;
      text-align: center;
      color: var(--gray-700);
      font-size: 14px;
    }

    .footer strong {
      color: var(--black);
    }

    .large {
      font-size: 1.25em;
    }

    .large .page {
      width: min(100%, 560px);
    }

    .large h1 {
      font-size: 42px;
    }

    .large .button {
      font-size: 1.2em;
      padding: 20px 16px;
    }
  </style>
</head>

<body>
  <a class="skip-link" href="#main">본문으로 바로가기</a>

  <main id="main" class="page">
    <section class="hero" aria-labelledby="page-title">
      <div class="badge">#찾았닷 #두다닷 챌린지</div>
      <h1 id="page-title">찾았닷!</h1>
      <p>일상 속 점자를 발견하고, 읽고, 기록해보세요.</p>

      <div class="search-box" aria-hidden="true">
        <span>N</span>
        <span>찾았닷 점자프로젝트</span>
      </div>
    </section>

    <button class="button secondary" type="button" onclick="toggleLarge(this)" data-original="큰 글씨로 보기">
      큰 글씨로 보기
    </button>

    <section class="section" aria-labelledby="braille-chart-title">
      <h2 id="braille-chart-title">점자 읽어보기</h2>
      <p>처음 보는 사람도 쉽게 볼 수 있도록 기본 자음, 모음, 숫자 중심으로 정리합니다.</p>

      <div class="braille-card">
        <!-- 아래 src 파일명만 나중에 바꾸면 됩니다. 예: images/korean-braille-chart.png -->
        <img src="braille-chart.png" alt="한글 점자 일람표 이미지" onerror="this.style.display='none'; this.nextElementSibling.style.display='block';" />
        <div class="placeholder" style="display:none;">
          한글점자일람표 이미지가 들어갈 자리입니다.<br />
          파일명을 <strong>braille-chart.png</strong>로 올리면 자동 표시됩니다.
        </div>
      </div>

      <details>
        <summary>입문형 점자표에는 무엇을 넣으면 좋을까요?</summary>
        <p>기본 자음, 기본 모음, 숫자, 이름 읽기 예시 정도만 먼저 넣는 것을 추천합니다. 문장부호, 수학기호, 약자는 별도 자료로 빼는 편이 체험용으로 더 좋습니다.</p>
      </details>
    </section>

    <section class="section" aria-labelledby="translator-title">
      <h2 id="translator-title">내 이름 점자로 보기</h2>
      <p>점자 번역기에서 내 이름이나 짧은 문장을 점자로 바꿔볼 수 있습니다.</p>
      <a class="button green" href="https://www.braillekorea.org/" target="_blank" rel="noopener noreferrer">
        점자로 번역해보기 →
      </a>
      <p style="margin-top:8px; font-size:13px;">※ 연결 주소는 하상장애인복지관 점자세상 주소로 교체해주세요.</p>
    </section>

    <section class="section challenge" aria-labelledby="challenge-title">
      <h2 id="challenge-title">찾았닷 챌린지</h2>
      <p class="tagline">#찾았닷 #두다닷 챌린지에 참여해보세요!</p>
      <ul class="mini-list">
        <li>1. 일상 속 점자를 발견해요</li>
        <li>2. 사진으로 기록해요</li>
        <li>3. 스토리에 가볍게 공유해요</li>
      </ul>
    </section>

    <section class="section" aria-labelledby="report-title">
      <h2 id="report-title">점자 오류를 발견했나요?</h2>
      <p>잘못된 점자, 없는 점자, 읽기 어려운 점자를 발견하면 알려주세요. 더 나은 접근성을 만드는 자료가 됩니다.</p>
      <a class="button" href="https://naver.me/xB7DoOjr" target="_blank" rel="noopener noreferrer">
        점자 오류 제보하기 →
      </a>
    </section>

    <section class="section" aria-labelledby="about-title">
      <h2 id="about-title">두다닷은 무엇을 하나요?</h2>
      <p>두다닷은 점자를 중심으로 시각장애인의 권리 증진과 문화예술 참여를 함께 만들어가는 비영리 임의단체입니다.</p>
      <a class="button secondary" href="about.html">
        단체 소개 보기 →
      </a>
    </section>

    <footer class="footer">
      <p><strong>DODADOT 두다닷</strong></p>
      <p>문의: dodadot@naver.com</p>
    </footer>
  </main>

  <script>
    function toggleLarge(btn) {
      document.body.classList.toggle('large');

      if (document.body.classList.contains('large')) {
        btn.innerText = '기본 크기로 돌아가기';
      } else {
        btn.innerText = btn.getAttribute('data-original');
      }
    }
  </script>
</body>
</html>
